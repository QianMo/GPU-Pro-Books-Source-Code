Because this sample is restricted to the reference device, it only fills a
texture with random numbers, that represent the entity IDs and builds
a histogram of those. After execution, a file named result.txt is written 
to disk.